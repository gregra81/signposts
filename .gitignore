node_modules/
dist/
.stryker-tmp/
reports/
coverage/

# Build output: the pre-compiled hook bundle (scripts/build-hooks.mjs).
hooks/*.js
statusline/*.js

# Release artefacts: `pnpm pack --pack-destination dist-release`, built in CI on a tag.
dist-release/
*.tgz
