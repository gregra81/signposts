// Placeholder entry point for src/io/model.
// This is the only sanctioned home for the "@anthropic-ai/sdk" import
// (enforced by the local eslint rule "signposts/no-anthropic-sdk-outside-io-model").
export type Placeholder = never;
